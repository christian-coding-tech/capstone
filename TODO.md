# TODO

- [x] Brainstorm and confirm edit plan to add a popup/modal that opens when clicking the Navigation button, and make the 3D model “ready” for interaction.
- [x] Confirmed: “ready 3d model” = existing temporary image `img/3d pic.png`.
- [x] After approval: update `index.php` to add popup markup (modal/overlay) and wire Navigation button.
- [x] Update `css/login.css` to prevent background scroll when modal is open.
- [x] Update `js/login.js` to open/close the popup.
- [ ] Run a quick manual check by loading `index.php` in browser and verifying click behavior.

